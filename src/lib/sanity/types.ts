/**
 * Sanity types – stub. Run `npm run sanity:typegen` to generate types from studio schema (from repo root).
 */
export type {
  ProjectByLangResult,
  ProjectsIndexItem,
  ProjectBySlugResult,
} from "./queries";
